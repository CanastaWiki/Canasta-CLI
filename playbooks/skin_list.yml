---
# canasta skin list - List installed skins.

- name: List skins
  ansible.builtin.include_role:
    name: extensions_skins
    tasks_from: list.yml
  vars:
    _item_type: skins
    _item_dir: skins

kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: {{.ClusterName}}
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: {{.HTTPNodePort}}
    hostPort: {{.HTTPPort}}
    protocol: TCP
  - containerPort: {{.HTTPSNodePort}}
    hostPort: {{.HTTPSPort}}
    protocol: TCP

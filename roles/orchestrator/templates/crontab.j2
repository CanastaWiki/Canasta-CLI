# Auto-generated by Canasta-Ansible — DO NOT EDIT THIS FILE DIRECTLY.
# To add custom cron jobs, edit config/crontab.extras instead; this
# file is regenerated from it on every canasta restart.
#
# Runs inside the ghcr.io/canastawiki/canasta-cron container
# (supercronic). The container has docker-cli and a read-only docker
# socket, so entries can use `docker exec <container> <command>` or
# `docker run --rm ...` to invoke work.
#
# Schedule syntax: standard 5-field cron (minute hour day-of-month
# month day-of-week), evaluated in the TZ set on the cron service.

{% if _crontab_extras_content is defined and _crontab_extras_content | length > 0 %}
# --- User-defined jobs (config/crontab.extras) ---
{{ _crontab_extras_content }}
{% endif %}

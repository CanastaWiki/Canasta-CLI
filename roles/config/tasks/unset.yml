---
# canasta config unset - Remove configuration keys.
# Input: id (optional), keys (str: space-separated keys), no_restart (bool), force (bool)

- name: Resolve instance
  ansible.builtin.include_tasks: "{{ playbook_dir }}/roles/common/tasks/resolve_instance.yml"

- name: Validate, unapply, and remove each key
  ansible.builtin.include_tasks: _unset_single.yml
  loop: "{{ keys.split() }}"
  loop_control:
    loop_var: _config_key

- name: Regenerate Caddyfile after config change
  ansible.builtin.include_role:
    name: orchestrator
    tasks_from: update_config.yml

- name: Restart instance to apply changes
  when: not (no_restart | default(false) | bool)
  ansible.builtin.include_role:
    name: instance_lifecycle
    tasks_from: restart.yml

- name: Report keys removed
  ansible.builtin.debug:
    msg: "Keys removed.{{ (no_restart | default(false) | bool) | ternary(' Restart skipped.', '') }}"
